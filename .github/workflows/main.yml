name: Gladiator_Shield_V9_SecurityAudit
on: [workflow_dispatch, push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 8.0.2

      - name: Inject_Security_Audit_Logic
        run: |
          rm -rf app/src/main/*
          mkdir -p app/src/main/java/com/gladiator/shield
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/res/drawable

          # 1. New UI with Audit Report Section
          cat <<EOF > app/src/main/res/layout/activity_main.xml
          <?xml version="1.0" encoding="utf-8"?>
          <ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="#050505">
              <LinearLayout
                  android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:orientation="vertical"
                  android:padding="20dp">
                  
                  <TextView
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:text="GLADIATOR SECURITY AUDIT"
                      android:textColor="#D4AF37"
                      android:textSize="22sp"
                      android:textStyle="bold"
                      android:layout_gravity="center_horizontal"/>

                  <TextView
                      android:id="@+id/auditSummary"
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:textColor="#FFFFFF"
                      android:background="#1A1A1A"
                      android:padding="15dp"
                      android:layout_marginTop="20dp"
                      android:text="Generating Report..."/>

                  <TextView
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:text="Privacy Recommendations:"
                      android:textColor="#D4AF37"
                      android:layout_marginTop="20dp"
                      android:textStyle="italic"/>

                  <TextView
                      android:id="@+id/privacyTips"
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:textColor="#AAAAAA"
                      android:textSize="14sp"
                      android:layout_marginTop="10dp"/>

                  <View
                      android:layout_width="match_parent"
                      android:layout_height="1dp"
                      android:background="#D4AF37"
                      android:layout_marginVertical="15dp"/>

                  <TextView
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:text="Detailed Threat List:"
                      android:textColor="#D4AF37"/>

                  <LinearLayout
                      android:id="@+id/threatContainer"
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:orientation="vertical"
                      android:layout_marginTop="10dp"/>
              </LinearLayout>
          </ScrollView>
          EOF

          # 2. Advanced Audit Logic
          cat <<EOF > app/src/main/java/com/gladiator/shield/MainActivity.java
          package com.gladiator.shield;
          import android.app.Activity;
          import android.content.Intent;
          import android.net.Uri;
          import android.os.Bundle;
          import android.view.View;
          import android.widget.*;
          import android.content.pm.*;
          import java.util.*;

          public class MainActivity extends Activity {
              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  setContentView(R.layout.activity_main);
                  
                  startService(new Intent(this, GuardianService.class));
                  
                  TextView summary = findViewById(R.id.auditSummary);
                  TextView tips = findViewById(R.id.privacyTips);
                  LinearLayout container = findViewById(R.id.threatContainer);
                  
                  int riskCount = 0;
                  PackageManager pm = getPackageManager();
                  List<PackageInfo> packages = pm.getInstalledPackages(PackageManager.GET_PERMISSIONS);
                  
                  for (PackageInfo pi : packages) {
                      if (pi.requestedPermissions != null) {
                          for (String perm : pi.requestedPermissions) {
                              if (perm.equals("android.permission.CAMERA") || perm.equals("android.permission.RECORD_AUDIO")) {
                                  riskCount++;
                                  Button b = new Button(this);
                                  b.setText("🚨 " + pi.applicationInfo.loadLabel(pm));
                                  b.setBackgroundColor(0xFF1A1A1A);
                                  b.setTextColor(0xFFD4AF37);
                                  final String pkg = pi.packageName;
                                  b.setOnClickListener(v -> {
                                      Intent i = new Intent(android.provider.Settings.ACTION_APPLICATION_DETAILS_SETTINGS);
                                      i.setData(Uri.parse("package:" + pkg));
                                      startActivity(i);
                                  });
                                  container.addView(b);
                                  break;
                              }
                          }
                      }
                  }
                  
                  summary.setText("Audit Status: Completed\nSecurity Level: " + (riskCount > 5 ? "LOW" : "HIGH") + "\nApps Monitored: " + packages.size());
                  tips.setText("• Revoke unused camera permissions.\n• Monitor background mic activity.\n• Trust only Gladiator-verified apps.");
              }
          }
          EOF

          # 3. Maintain Guardian Service
          cat <<EOF > app/src/main/java/com/gladiator/shield/GuardianService.java
          package com.gladiator.shield;
          import android.app.Service;
          import android.content.Intent;
          import android.os.IBinder;

          public class GuardianService extends Service {
              @Override
              public int onStartCommand(Intent intent, int flags, int startId) { return START_STICKY; }
              @Override
              public IBinder onBind(Intent intent) { return null; }
          }
          EOF

          # 4. Manifest & Build (The Royal Standards)
          cat <<EOF > app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.gladiator.shield">
              <application android:label="Gladiator Shield" android:icon="@drawable/ic_shield_gold" android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
                  <service android:name=".GuardianService" android:enabled="true" android:exported="false" />
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          cat <<EOF > app/src/main/res/drawable/ic_shield_gold.xml
          <vector xmlns:android="http://schemas.android.com/apk/res/android" android:width="24dp" android:height="24dp" android:viewportWidth="24" android:viewportHeight="24"><path android:fillColor="#D4AF37" android:pathData="M12,1L3,5v6c0,5.55 3.84,10.74 9,12 5.16,-1.26 9,-6.45 9,-12V5l-9,-4z"/></vector>
          EOF

          echo "include ':app'" > settings.gradle
          cat <<EOF > app/build.gradle
          plugins { id 'com.android.application' }
          android { namespace 'com.gladiator.shield'; compileSdk 33; defaultConfig { applicationId "com.gladiator.shield"; minSdk 21; targetSdk 33 }
          compileOptions { sourceCompatibility JavaVersion.VERSION_17; targetCompatibility JavaVersion.VERSION_17 } }
          EOF

      - name: Build_Audit_V9
        run: gradle :app:assembleDebug --no-daemon

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: Gladiator-Shield-Audit-V9
          path: app/build/outputs/apk/debug/*.apk
